import React from "react";
// สร้าง ContextObject

// const context = React.createContext("thai");

// console.log(context);
// createContext จะ return Provider กับ Consumer ซึ่งเป็น react component เอาไว้ใช้งาน

export default React.createContext("thai");
// Source of data แบบ Default Value ใน ContextObject
